The prompt::
you are senior mnc backend engineer and you have to build the backend for a perfume sell/buy networked direct-selling marketplace with multi-level commission ledger and you are most experinced builder so you will implement it phase by phase i already created the phse by phse plans so start from phase 1::
-it is not a school project it is a real life project so
-don't give me fake details , you have to make everything professionally
and your main task is learn the phases and turn the phse by phase implementation to a MNC level prompt for my ai to build it
you should include all type of tests in each phases to check and make sure the phases are implemented correctly
you must not::
-hallucinate
-no fake codes or details
-no hard coded

so start from phase 1 and then move to next phases one by one

perfume buy/sell market place
name:hadi perfumes

The core idea::
networked direct-selling marketplace with multi-level commission ledger

Core systems:

Identity + sponsorship graph
Every user has a sponsor, and possibly an upline path.
Sales + order engine
Listings, checkout, payment capture, shipping, refunds.
Commission engine
Commission is calculated only from eligible, verified events.
Ledger engine
Every money movement is recorded as an immutable entry.
Compliance + fraud engine
KYC, anti-abuse, clawbacks, audit logs, earnings controls.
The safest backend rule

Do not pay commissions on:

signup alone
recruitment alone
rank upgrades alone
self-purchases that exist only to trigger rewards
fake or repeated orders

The FTC specifically warns about compensation structures that create incentives to recruit rather than sell to real end users, and about plans that reward purchases by participants without regard to genuine retail demand.

Recommended backend architecture
1) Identity and onboarding
User registers with phone/email.
Backend requires a valid sponsor/referral code.
Backend stores:
user_id
sponsor_id
upline_path
referral_code_used
signup_ip, device_fingerprint, created_at
Anti-abuse checks:
no self-referral
no circular sponsorship
one account per verified identity/device policy
cooldowns and rate limits

Inference: use a sponsorship tree table plus an immutable referral redemption log so you can audit who entered through whom and reverse bad joins safely.

2) Network graph

You need a way to query:

direct recruits
level 2, level 3, etc.
active downline volume
qualified personal sales
upline commission eligibility

Inference: store both:

adjacency (sponsor_id)
materialized path (upline_path)
so reads are fast and tree traversal stays manageable.
3) Product + inventory
Sellers create listings.
Each listing has:
SKU
price
quantity available
condition
authenticity status
seller status
Inventory must be reserved atomically at checkout.

Inference: use reservation states:

available
reserved
sold
returned
cancelled

Never decrement stock with a plain read-modify-write. Use transactional locking or an atomic update.

4) Order lifecycle

Typical states:

created
payment_pending
paid
packed
shipped
delivered
return_window_open
completed
refunded
disputed
chargeback

Inference: commissions should move from pending to available only after the order passes the risk/return window.

5) Ledger and wallet

This is the most important part.

Use an append-only ledger with entries like:

sale revenue
platform fee
seller payout
level-1 commission
level-2 commission
holdback
refund reversal
chargeback reversal
manual adjustment

Never store money as a single editable number without a transaction trail.

Inference: wallet balances should be derived from ledger state:

pending_balance
available_balance
held_balance
reversed_balance
6) Commission engine

A safe structure is:

Order is paid.
Commission is calculated.
Commission is written as pending.
After delivery + return window, it becomes available.
If refund/chargeback happens, the system posts a clawback.

Inference: make commission rules versioned:

rule version
level percentage
product category eligibility
seller eligibility
caps
payout delay
refund clawback window

This prevents old orders from breaking when you change the plan.

7) Admin and fraud controls

You need:

user verification review
seller approval
suspicious network review
earnings anomaly detection
refund abuse monitoring
manual commission holds
blacklists / watchlists
audit logs for every admin action

The FTC’s guidance emphasizes that deceptive earnings claims and misleading representations are core risks in MLMs, and that such claims must be substantiated and not exaggerate what typical participants earn.

Suggested database schema outline

Users

id
name
email
phone
status
role
sponsor_id
created_at

ReferralCodes

id
owner_user_id
code
status
max_uses
expires_at

ReferralRedemptions

id
new_user_id
sponsor_user_id
code
ip
device_hash
created_at

SellerProfiles

user_id
kyc_status
payout_method
risk_score

Listings

id
seller_id
title
sku
price
quantity
condition
status

InventoryReservations

id
listing_id
order_id
qty
expires_at
status

Orders

id
buyer_id
status
subtotal
shipping_fee
tax
total
created_at

OrderItems

id
order_id
listing_id
seller_id
qty
price

Payments

id
order_id
provider
provider_txn_id
status
captured_at

CommissionRules

id
version
level
percentage
min_order_value
eligible_product_rules
effective_from
effective_to

LedgerEntries

id
user_id
order_id
entry_type
amount
currency
status
reference_id
created_at

Wallets

user_id
pending_balance
available_balance
held_balance

Payouts

id
user_id
amount
status
destination
requested_at
processed_at

Disputes

id
order_id
opened_by
reason
status

AuditLogs

id
actor_user_id
action
target_type
target_id
metadata
created_at
Key APIs you would need
POST /auth/signup
POST /referrals/validate
POST /sellers/apply
POST /listings
POST /inventory/reserve
POST /orders
POST /payments/capture
POST /commissions/calculate
POST /wallets/payout-request
POST /orders/{id}/refund
POST /disputes
POST /admin/commission-hold
GET /network/downline
GET /wallet/ledger
Edge cases and failure modes
referral code reused across fake accounts
sponsor changes after signup
seller refunds after commission payout
duplicate payment callbacks
inventory oversell under concurrency
chargeback after payout
self-purchase abuse
downline cycles
admin manual edits without audit trail
earnings claims shown in UI without substantiation
Do not copy these risky patterns
paying mainly for recruitment
“guaranteed income” messaging
rank advancement that requires endless self-buying
hidden or unclear commission rules
editable wallet balances with no ledger
no clawback on refunded orders
no audit log for admin actions
no identity checks for high-volume earners
no limit on repeated new-account bonuses
vague terms around “active user” or “qualified sale”

The FTC has also said that earnings claims must reflect typical participant outcomes and expenses, and that omitting losses or inactive participants can be deceptive.

What you should build first
Referral onboarding + sponsor graph
Order + payment + inventory flow
Ledger-based wallet
Commission calculation engine
Refund/clawback logic
Admin review dashboard
Fraud detection and audit logs
Concise executive summary

Your project is best treated as a direct-selling marketplace with a multi-level commission ledger. The backend must be built around verified retail sales, immutable money records, and fraud/compliance controls. The biggest architectural mistake would be to treat recruitment rewards like simple referral bonuses. The FTC’s guidance makes clear that MLM legality turns on how the compensation structure works in practice, especially whether rewards are tied to sales to ultimate users rather than recruitment.

Top 10 most important insights
MLMs are organized by downlines and uplines.
Recruitment-based compensation is the main legal danger.
Earnings claims need substantiation and typical-outcome disclosure.
Wallets should be ledger-based, not editable balances.
Commissions should be pending until delivery and return-window expiry.
Refunds need clawbacks.
Every referral must be auditable.
Inventory must be reserved atomically.
Admin actions need logs.
Compliance is part of the backend, not a legal afterthought.
Top 10 risks for your backend
Pyramid-scheme classification risk.
False earnings claims.
Fake-account referral abuse.
Self-purchase abuse.
Commission clawback failure after refunds.
Oversold inventory.
Duplicate payment/webhook processing.
Manual admin changes without audit trails.
Weak seller/buyer trust controls.
Upline/downline graph corruption.
10 best follow-up questions to ask next
Should commissions be 2-level, 3-level, or deeper?
What counts as a “qualified sale”?
Should commissions be paid only after delivery?
How do we define fake account abuse?
Should there be KYC for sellers or top earners?
How should inventory be reserved under concurrency?
Should commissions be reversed on refunds or chargebacks?
What admin approvals are required before payouts?
How should downline trees be stored for fast queries?
What compliance disclosures must the UI show before signup?
The backend phases you should build
Phase 1: Business rules, compensation model, and compliance boundaries

This is where you define the money logic before writing product code. Public guidance from the FTC flags recruitment-heavy incentives, threshold-based rewards, convex rewards, duplication rewards, and inventory loading as red flags. Amway’s public help material also frames its compensation around selling products to customers and helping sponsored business owners do the same.

Build here

commission levels
qualification rules
payout delay rules
refund/clawback rules
rank rules
allowed earnings claims
compliance disclosure rules

Recommended stack

start with plain config tables in PostgreSQL
version the compensation plan
keep legal text and rule text in an admin CMS or config service

Safety terms

commission must be tied to verified retail sales
define “qualified sale” before any payout
no rewards for signup alone
no rewards for recruitment alone
no rank upgrades based only on self-purchase volume

Do not do

hidden volume thresholds
vague “income opportunity” wording
rewards that can be earned without real retail demand
one-off manual payout math in code

Examples

Amway: public compensation plan and sponsored-downline structure.
DSA member companies: direct-selling model with compensation based on personal sales and/or sales of others.
Phase 2: Identity, onboarding, and referral validation

This is the gated signup layer. The user cannot complete signup until the referral/sponsor code is validated.

Build here

invite/referral code service
signup flow
sponsor assignment
one-time-use or bounded-use codes
device/IP/rate-limit checks
identity verification for higher-risk users

Recommended stack

auth: email/phone OTP plus password or passkeys
backend endpoint: POST /referrals/validate
store referral_code_used, sponsor_id, signup_ip, device_hash, verified_at

Safety terms

referral code must be validated server-side
prevent self-referral
prevent circular sponsorship
freeze sponsor relationships after signup unless there is a formal correction flow
require stronger verification for sellers and top earners

Do not do

trust the frontend to enforce invite rules
let users edit their sponsor later without audit
allow mass code guessing without rate limits
store referral logic only in cookies or client state

Examples

MLM/direct selling platforms: sponsor/downline onboarding is structurally normal.
Stripe Connect onboarding shows the modern pattern of verification-heavy multi-party onboarding, even though it is payments-focused rather than MLM-focused.
Phase 3: Network graph and qualification engine

This is the part that makes multi-level commissions possible.

Build here

upline/downline tree
level depth rules
qualification status
active/inactive participant status
personal sales volume
downline sales volume
rank and eligibility engine

Recommended stack

PostgreSQL adjacency list plus materialized path
background jobs for qualification recalculation
read-optimized views or cache for sponsor tree queries

Safety terms

keep the graph immutable except for audited corrections
record who sponsored whom at signup time
calculate commissions from events, not from a mutable balance
store rule version with each commission decision

Do not do

recursive ad hoc logic in every API call
recompute the whole tree on every request
let ranks depend only on recruitment count
use undefined “volume” fields that mix retail and participant purchases

Examples

Amway explicitly describes earning through sales and helping sponsored owners do the same.
The FTC guidance discusses downlines and multiple participant levels as the core MLM structure.
Phase 4: Catalog, seller accounts, and inventory

This is the normal marketplace layer.

Build here

seller profile
product listing
SKU management
stock quantity
listing approval
media uploads
condition/authenticity fields
search/filter

Recommended stack

product service in NestJS / Django / Spring, whichever you prefer
PostgreSQL for source of truth
object storage for images
Redis for hot listing cache
search engine later if needed

Safety terms

inventory must be reserved transactionally
listing ownership must be clear
perfume authenticity/condition should be explicit
require seller verification for higher-risk categories

Do not do

decrement stock without locking/reservation
allow duplicate listings for the same inventory item without a policy
let sellers change price/quantity mid-checkout without rules
allow unverified sellers to sell high-risk items without review

Examples

Sharetribe product marketplace supports seller profiles, customizable user types, inventory management, secure payments, messaging, and reviews.
Etsy is a strong real-world example of seller-managed listings and shop-level policies.
Phase 5: Orders, checkout, payments, and money movement

This is where you decide whether you are the merchant of record and how money flows.

Build here

cart/checkout
order creation
payment intent
payment capture
seller split
platform fee
buyer receipt
webhook handling

Recommended stack

Stripe Connect for multi-party payments
idempotent order and payment endpoints
asynchronous webhook processor
background reconciliation jobs

Stripe Connect is designed for platforms and marketplaces, with onboarding, account management, payments, payouts, risk management, and tax reporting. Stripe’s marketplace docs describe the pattern of customers paying the marketplace and the marketplace paying sellers, and it supports separate charges and transfers when funds must be split across multiple parties.

Safety terms

every payment API must be idempotent
every webhook must be deduplicated
use authorization/capture properly
do not release seller or upline funds until the payment is settled and fraud checks pass

Do not do

create payouts before payment confirmation
rely on a single webhook event as final truth
update order status manually outside audit flow
let the same order trigger commission twice

Examples

Stripe Connect marketplaces such as Shopify, DoorDash, and Lyft are shown by Stripe as real-world platform examples.
Etsy shows the order-status and help-request model buyers actually expect.
Phase 6: Commission ledger, wallet, payout, and clawbacks

This is the most important non-brittle part of your backend.

Build here

immutable ledger entries
pending balance
available balance
held balance
payout requests
payout batches
reversal and clawback entries
commission rule versioning

Stripe’s payouts docs show the same general pattern: funds accumulate in balances, can be held or paid out on a schedule, and payouts can fail and be tracked through webhooks. Stripe also documents separate charges and transfers, where the platform charge is decoupled from transfers to connected accounts.

Safety terms

never store money as a simple editable number only
every balance change needs a ledger entry
commissions should become payable only after delivery and return window expiry
refunds and chargebacks must reverse commissions automatically

Do not do

wallet.balance += amount as your only source of truth
pay out instantly on recruitment
ignore chargebacks after payout
use one hardcoded commission rule forever

Examples

Stripe Connect payouts and balance timing are the closest public payment-pattern reference.
Phase 7: Disputes, returns, moderation, and fraud

This is your trust layer.

Build here

return request flow
dispute case flow
seller response workflow
buyer protection logic
admin review queue
fraud scoring
seller suspension
refund authorization

Etsy’s help center shows a strong example: sellers manage their own orders and policies, buyers can open a case, and qualifying orders may get refunds for nondelivery, damage, or items not matching the description.

Safety terms

every admin action must be logged
seller decisions need policy evidence
disputes should freeze payouts
fraud cases should freeze commissions until resolved

Do not do

let support manually alter balances without trace
allow disputes to close without a reason code
let repeated abuse continue without a risk score
hide refund rules from sellers and buyers

Examples

Etsy Purchase Protection and case handling are a good public model for buyer protection.
Phase 8: Observability, security, and scaling

This phase keeps the system sane under real load.

Build here

audit logs
metrics and traces
anomaly detection
dead-letter queues
retry strategy
idempotency keys
secret management
permissioned admin panels

Stripe explicitly recommends idempotent requests for safe retries, so duplicate network calls do not create duplicate objects or double actions.

Recommended stack

Redis / queue system for async work
structured logs
metrics dashboard
object-level permissions
role-based admin access
alerting on payout failures and commission spikes

Safety terms

every state transition should be auditable
use idempotency keys for orders, payouts, and commission generation
separate synchronous user actions from asynchronous settlement
keep secrets out of the app code

Do not do

make money-moving actions synchronous only
ignore webhook retries
reuse the same request for different business actions
allow full admin power to every support user

Examples

Stripe idempotency and webhook design is the best public pattern here.
Recommended tech stack for your project

This is my recommendation, not a public fact about those companies:

Frontend

Next.js
TypeScript
Tailwind

Backend

NestJS or Fastify with TypeScript
PostgreSQL as the main database
Redis for caching, rate limits, and job coordination
BullMQ or another queue for async tasks
S3-compatible object storage for images/docs
Stripe Connect for marketplace payouts
Webhook worker service
Audit-log service
Optional search engine later

Why this stack

strong type safety for money flows
good fit for event-driven workflows
easy to build ledger + commission logic
easy to grow into microservices later if needed
Suggested database shape

At minimum, build these tables:

users
roles
referral_codes
referral_redemptions
sponsorship_tree
seller_profiles
listings
listing_images
inventory_reservations
orders
order_items
payments
refunds
commission_rules
commission_events
ledger_entries
wallets
payouts
disputes
audit_logs
fraud_signals

The critical design choice is that wallets are derived from ledger entries, not the other way around.

Things you should not do
do not pay commissions on signup alone
do not pay mainly for recruitment
do not allow unverified sponsor changes
do not keep balances without a ledger
do not ignore refund reversals
do not let inventory update without transactions
do not trust client-side referral validation
do not allow “earn fast” claims without actual evidence
do not make rank require constant self-buying
do not skip audit logs for admin actions

The FTC specifically warns that MLM structures can become problematic when incentives push participants toward recruiting, threshold purchases, or inventory loading rather than sales to genuine end users.
==================================================================================================================Phase 1 build prompt in an MNC-grade way for your AI builder.

You are a senior backend architect building the production backend for Hadi Perfumes, a real-world perfume buy/sell networked direct-selling marketplace with a multi-level commission ledger.

Your task in this phase is ONLY Phase 1:
Business rules, compensation model, and compliance boundaries.

Do not build auth, onboarding, products, orders, payments, wallet movement, or payout execution in this phase unless absolutely required to support the rule engine as a thin foundation.
Do not invent business rules.
Do not hardcode percentages, thresholds, rank logic, payout delay numbers, or legal/compliance text.
All rules must be configurable, versioned, and auditable.
The system must be designed for real money, real users, real audits, and future legal review.

Primary objective:
Create a versioned compensation-policy foundation that defines what kinds of sales/events are eligible for commission, when commissions can become payable, what must be blocked, how clawbacks should behave, and what compliance disclosures must be enforced.

Core principles:
1. Commissions must only be tied to verified retail sales events.
2. No payout for signup alone.
3. No payout for recruitment alone.
4. No payout for rank upgrades alone.
5. No payout for self-purchases that exist only to trigger rewards.
6. No payout for fake, repeated, or manipulated orders.
7. Every compensation rule must be versioned and immutable after activation.
8. Past orders must remain governed by the rule version active at the time of the event.
9. Every rule change must create an audit trail.
10. Compliance text and allowed earnings claims must be explicitly stored and reviewable.

Phase 1 deliverables:
A. Compensation policy model
Design a policy engine that can represent:
- commission levels
- eligibility rules
- payout delay rules
- refund/clawback rules
- rank qualification rules
- product/category eligibility rules
- seller eligibility rules
- caps and limits
- prohibited triggers
- earnings-claim/disclosure rules
- effective-from and effective-to versioning

B. Data model
Create a PostgreSQL-first schema for a versioned policy system.
At minimum, define tables or equivalent domain entities for:
- compensation_plan_versions
- commission_rules
- rank_rules
- payout_rules
- clawback_rules
- eligibility_rules
- prohibited_event_rules
- compliance_disclosures
- allowed_earnings_claims
- rule_audit_logs
- admin_policy_changes

Each rule set must be:
- versioned
- immutable after activation
- linked to the admin action that created it
- queryable by effective date and event type

C. Validation layer
Implement server-side validation for:
- missing fields
- invalid version transitions
- overlapping effective dates
- impossible rule combinations
- negative or nonsensical percentages/limits
- prohibited reward logic
- circular or contradictory rule references
- missing disclosures where required

D. Policy evaluation layer
Implement pure backend logic that can answer:
- Is this event commission-eligible?
- Which rule version applies?
- Is this payout delayed or held?
- Is this event excluded by compliance policy?
- Is this event clawback-eligible later?
- Is this claim/disclosure allowed to be shown?

This logic must be deterministic, testable, and independent of UI code.

E. Admin change workflow
Build a safe admin flow for policy updates:
- draft version
- validation
- review/approval hook
- activation
- deprecation of prior version
- full audit log of who changed what and when

F. Compliance boundaries
The system must explicitly support:
- allowed earnings claims
- required disclosures
- prohibited promotional statements
- prohibited incentive types
- auditability of all compliance text changes

Implementation constraints:
- Use PostgreSQL as the source of truth.
- Keep business rules in config tables or policy entities, not hardcoded in services.
- Use clear domain naming.
- Keep the policy engine pure where possible.
- Every state change must be auditable.
- Avoid mixing legal content, business logic, and UI content in one blob.
- Design for future expansion to more phases without rewriting this foundation.

Required APIs for this phase:
Implement only the minimal admin and read APIs needed to manage and inspect the compensation policy.

Examples:
- GET /admin/compensation-policy/current
- GET /admin/compensation-policy/versions
- POST /admin/compensation-policy/drafts
- POST /admin/compensation-policy/drafts/{id}/validate
- POST /admin/compensation-policy/drafts/{id}/activate
- GET /admin/compensation-policy/{versionId}
- GET /admin/compliance/disclosures
- GET /admin/compliance/allowed-claims

Testing requirements:
You must include a complete test suite for this phase.

1. Unit tests
Cover:
- rule validation
- version activation rules
- eligibility evaluation
- payout delay evaluation
- clawback rule evaluation
- disclosure selection
- prohibited-event rejection
- immutability checks

2. Integration tests
Cover:
- creating a draft policy
- validating a draft policy
- activating a policy version
- fetching the active policy
- confirming old versions remain unchanged
- confirming audit logs are written correctly

3. Contract/API tests
Cover:
- request/response schema validation
- unauthorized admin access rejection
- invalid payload handling
- version lookup behavior
- backward-compatible response structure

4. Property-based / invariant tests
Verify invariants such as:
- a disallowed event can never become commission-eligible
- an activated rule version cannot be mutated
- a policy version cannot have overlapping effective periods
- the same event always resolves to the same rule version
- no payout rule can bypass compliance exclusions

5. Regression tests
Create tests for:
- blocked recruitment-only rewards
- blocked signup-only rewards
- blocked self-purchase reward triggers
- blocked rank-upgrade-only rewards
- blocked hidden thresholds or undefined reward logic
- blocked rule edits without audit logs

6. Security tests
Cover:
- access control on admin endpoints
- tamper attempts on activated versions
- privilege escalation on policy edit actions
- audit log integrity

7. Migration tests
If versioned policy tables require migrations:
- ensure schema upgrades do not alter historical interpretations
- ensure old policy versions remain readable
- ensure activation logic still works after migration

Definition of done:
- Compensation policy is fully versioned and auditable.
- All rule decisions are server-side and deterministic.
- No hardcoded business logic exists in the service layer.
- All prohibited reward patterns are explicitly blocked.
- Compliance text and allowed claims are stored and retrievable.
- Admin changes are tracked end-to-end.
- Tests prove the system rejects unsafe compensation structures.

Output expectations:
1. Database schema or migrations
2. Domain model
3. Policy validation logic
4. Policy evaluation logic
5. Admin policy APIs
6. Full test suite
7. Clear documentation of rule semantics
8. Notes on any assumptions or unresolved business decisions that require product/legal confirmation

Do not proceed to later phases in this implementation.
Do not invent future-phase systems.
Build Phase 1 as if it will be used in production immediately.
======================================================================================================================
PHASE 2 BUILD PROMPT
You are a senior backend architect building the production backend for Hadi Perfumes, a real-world perfume buy/sell networked direct-selling marketplace with a multi-level commission ledger.

Your task in this phase is ONLY Phase 2:
Identity, onboarding, and referral validation.

Do not build products, orders, payments, inventory, wallet movement, commission calculation, or payout execution in this phase except for the minimum foundation strictly needed to support onboarding integrity.
Do not invent business rules.
Do not hardcode referral limits, sponsor logic, device policies, or identity thresholds.
All onboarding rules must be configurable, versioned where needed, and auditable.
The system must be safe for real users, real referral abuse, and future compliance review.

Primary objective:
Build a gated onboarding system where a user cannot complete signup until the referral/sponsor relationship is validated on the server, stored immutably, and protected against abuse.

Core principles:
1. Referral validation must happen server-side.
2. No self-referral.
3. No circular sponsorship.
4. No sponsor editing after signup unless a formal audited correction flow exists.
5. Every referral redemption must be recorded immutably.
6. Every onboarding action must be auditable.
7. Identity verification must be supported for higher-risk users.
8. The backend must be resistant to referral-code guessing, abuse, and duplicate accounts.
9. Sponsorship data must support future network-tree queries without rewriting onboarding.
10. Referral relationships must be trustworthy enough to support later commission calculations.

Phase 2 deliverables:
A. Referral validation service
Design a backend service that can:
- validate a referral/sponsor code
- verify code status
- enforce code usage policy
- prevent self-referral
- prevent circular sponsorship
- prevent invalid or expired codes
- record the validation result before final signup completion
- support one-time-use or bounded-use referral codes
- reject duplicate or replayed validation attempts

B. Signup onboarding flow
Design the onboarding flow so that:
- a user submits signup data and referral code
- the backend validates the referral code before account creation is finalized
- sponsor assignment is stored on the server
- a valid sponsorship link is persisted only once
- the user cannot bypass referral checks from the client
- onboarding can be resumed safely if the process is interrupted

C. Identity and anti-abuse controls
Implement onboarding protections for:
- rate limiting
- IP tracking
- device fingerprint tracking
- suspicious signup bursts
- repeated code guesses
- duplicate account patterns
- policy-based identity verification for higher-risk users
- audit logging for blocked attempts

D. Data model
Create a PostgreSQL-first schema or equivalent entities for:
- users
- referral_codes
- referral_redemptions
- sponsorship_links or sponsorship_tree
- onboarding_attempts
- identity_verification_requests
- onboarding_audit_logs

Each entity must support:
- traceability
- immutability where needed
- safe correction flow
- future network graph queries

E. Sponsor assignment rules
Implement safe sponsor assignment semantics:
- sponsor is assigned at onboarding time
- sponsor relationship is frozen after signup unless corrected through an admin/audited workflow
- referral redemption records must preserve who referred whom
- correction flow must never silently rewrite history

F. Validation layer
Implement server-side validation for:
- missing referral code
- invalid code format
- expired referral code
- disabled referral code
- exhausted code usage limit
- self-referral
- circular sponsorship
- duplicate redemption
- suspicious replay attempts
- account state conflicts

G. Admin correction workflow
Build a controlled admin path for rare sponsor correction cases:
- reason required
- approval required
- full audit log
- no silent mutation
- historical trace preserved

H. Read APIs
Implement only the minimal APIs needed for this phase:
- POST /referrals/validate
- POST /auth/signup
- GET /me/onboarding-status
- GET /admin/referrals/{userId}
- POST /admin/referrals/{userId}/correct
- GET /admin/referral-codes/{code}
- GET /admin/onboarding-attempts

Implementation constraints:
- Use PostgreSQL as source of truth.
- Keep the referral validation logic server-side only.
- Do not rely on cookies or client state for sponsor truth.
- Do not trust the frontend to enforce onboarding rules.
- Every mutation must be auditable.
- Use clear domain naming.
- Keep the onboarding service deterministic and testable.
- Design for future Phase 3 network graph needs without overengineering.

Testing requirements:
You must include a complete test suite for this phase.

1. Unit tests
Cover:
- referral code validation
- expired/disabled code rejection
- self-referral rejection
- circular sponsorship rejection
- usage-limit enforcement
- sponsor assignment semantics
- onboarding state transitions
- correction rule validation

2. Integration tests
Cover:
- validating a good referral code
- rejecting invalid/expired/reused codes
- completing signup with a valid sponsor
- preventing sponsor changes after signup
- recording redemption and audit logs
- handling interrupted onboarding and resume behavior

3. Contract/API tests
Cover:
- request/response schema validation
- invalid payload handling
- unauthorized admin access rejection
- status lookup behavior
- backward-compatible response structure

4. Property-based / invariant tests
Verify invariants such as:
- one completed user has at most one sponsor assignment
- a code redemption cannot create a sponsorship cycle
- a redempted code cannot be reused beyond policy
- the same referral event always resolves to the same sponsor
- no client-side input can override server-side sponsor truth

5. Regression tests
Create tests for:
- self-referral abuse
- mass code guessing
- duplicate redemption replay
- sponsor editing after signup
- broken correction flow that loses audit history
- duplicate onboarding submits

6. Security tests
Cover:
- rate-limit enforcement
- IP/device abuse detection
- privilege escalation on admin correction endpoints
- tampering with sponsor fields
- replay attacks on referral validation
- audit log integrity

7. Migration tests
If schema migrations are required:
- ensure historical referral redemptions remain readable
- ensure sponsor assignments remain stable
- ensure correction records do not corrupt prior history
- ensure onboarding states still resolve correctly after migration

Definition of done:
- Referral validation is server-side and audited.
- Sponsor assignment is frozen after signup except through an audited correction process.
- Abuse controls block obvious fake-account and code-guessing patterns.
- The backend can safely support future network-graph and commission phases.
- Every onboarding action is traceable.
- Tests prove the system rejects invalid referral and sponsorship behavior.

Output expectations:
1. Database schema or migrations
2. Domain model
3. Referral validation logic
4. Signup onboarding flow
5. Admin correction APIs
6. Full test suite
7. Clear documentation of onboarding rules
8. Notes on any assumptions or unresolved product/compliance decisions that require approval

Do not proceed to later phases in this implementation.
Do not invent future-phase systems.
Build Phase 2 as if it will be used in production immediately.
====================================================================
phase 3 build prompt
You are a senior MNC backend architect building the production backend for Hadi Perfumes, a real-world perfume buy/sell networked direct-selling marketplace with a multi-level commission ledger.

Your task in this phase is ONLY Phase 3:
Network graph and qualification engine.

Do not build catalog, seller accounts, inventory, orders, payments, wallet movement, payout execution, or dispute systems in this phase except for the minimum read-only hooks needed to evaluate qualification state.
Do not invent business rules.
Do not hardcode level counts, qualification thresholds, rank logic, volume formulas, or activation rules.
All network and qualification rules must be configurable, versioned where needed, and auditable.
The system must be safe for real sponsorship trees, real multi-level traversal, and future commission settlement.

Primary objective:
Build a reliable, auditable sponsorship graph and qualification engine that can answer who is in whose downline, who is in whose upline, whether a participant is active, and whether they qualify for commission eligibility under the current rule set.

Core principles:
1. The sponsorship graph must be immutable except through audited correction flows.
2. Sponsor assignments come from Phase 2 and are treated as source-of-truth inputs.
3. Upline and downline queries must be fast, deterministic, and server-side.
4. Qualification must be based on verified sales and rule-driven volume logic, not recruitment alone.
5. Rank progression must never depend on recruitment alone.
6. All graph state changes must be auditable.
7. Commission eligibility must reference stored rule versions, not mutable runtime guesses.
8. Cycle prevention and path consistency are mandatory.
9. The graph must support future multi-level commission calculation without redesign.
10. The engine must be resilient to stale reads, concurrent updates, and repeated recalculation jobs.

Phase 3 deliverables:
A. Sponsorship graph model
Design a backend model that supports:
- sponsor_id adjacency
- materialized upline_path
- depth
- direct recruits
- downline traversal
- ancestor traversal
- correction-safe graph updates
- immutable historical sponsor records
- future commission-tree queries

B. Qualification engine
Implement a rule-driven engine that can determine:
- whether a user is active
- whether a user is qualified for commission receipt
- whether a user meets personal sales requirements
- whether a user meets downline sales requirements
- whether a user meets rank requirements
- whether a user is temporarily suspended from qualification
- whether a rule version applies to a user at evaluation time

C. Rank and level logic
Implement a versioned model for:
- rank definitions
- rank thresholds
- commission eligibility thresholds
- qualification refresh windows
- sales-volume calculations
- direct and indirect qualification paths

Important:
- Rank must be sales-based, not recruitment-based.
- Do not mix participant purchases and retail sales unless the policy explicitly allows it.
- Keep participant versus retail volume separated.

D. Data model
Create PostgreSQL-first schema or equivalent entities for:
- sponsorship_tree
- network_edges
- network_snapshots
- qualification_rules
- qualification_events
- qualification_states
- rank_definitions
- rank_assignments
- graph_rebuild_jobs
- graph_correction_logs

Each entity must support:
- traceability
- immutable historical records where required
- safe correction flow
- performant reads for downline/upline traversal

E. Graph maintenance
Implement safe mechanisms for:
- creating graph edges from Phase 2 sponsor data
- preventing circular references
- rebuilding materialized paths
- recalculating qualification state
- handling delayed or corrected onboarding data
- auditing graph corrections

F. Read APIs
Implement the minimal APIs needed for this phase:
- GET /network/upline
- GET /network/downline
- GET /network/stats
- GET /network/qualification-status
- GET /admin/network/corrections
- POST /admin/network/corrections
- POST /admin/network/recalculate
- GET /admin/network/snapshots

Implementation constraints:
- Use PostgreSQL as source of truth.
- Keep adjacency and materialized path both available if needed for performance.
- Do not compute graph truth only in memory.
- Do not trust client-provided hierarchy data.
- Every mutation must be auditable.
- Use clear domain naming.
- Keep the qualification engine deterministic and testable.
- Design for future commission settlement and payout logic without overengineering.

Testing requirements:
You must include a complete test suite for this phase.

1. Unit tests
Cover:
- upline/downline traversal
- path generation
- cycle detection
- qualification evaluation
- active/inactive state resolution
- rank threshold checks
- sales-volume calculation
- correction validation

2. Integration tests
Cover:
- creating a sponsorship edge from onboarding data
- building the materialized path
- rejecting circular graph updates
- recalculating qualification state
- applying a graph correction with audit logging
- restoring consistency after a rebuild job

3. Contract/API tests
Cover:
- request/response schema validation
- invalid payload handling
- unauthorized admin access rejection
- pagination and filter behavior
- backward-compatible response structure

4. Property-based / invariant tests
Verify invariants such as:
- a user can have at most one sponsor edge
- the graph cannot contain a cycle
- the upline path always ends at the root ancestors in the correct order
- the same graph state always produces the same qualification result
- rank cannot be awarded from recruitment count alone
- a correction cannot silently erase historical structure

5. Regression tests
Create tests for:
- cycle corruption during correction
- stale upline_path values
- duplicate graph edges
- wrong rank assignment after recalculation
- recruitment-only qualification attempts
- broken snapshot rebuilds

6. Security tests
Cover:
- tampering with sponsor or path fields
- privilege escalation on admin graph correction endpoints
- audit log integrity for graph mutation actions
- replay of graph correction requests
- unauthorized qualification-state reads if protected by role

7. Migration tests
If schema migrations are required:
- ensure old sponsorship records remain readable
- ensure materialized path backfills do not corrupt hierarchy
- ensure qualification history remains stable after migration
- ensure rank assignments still resolve correctly after schema change

Definition of done:
- The sponsorship graph is reliable, auditable, and cycle-free.
- Upline/downline traversal works for arbitrary depth under configured limits.
- Qualification state is computed from rules and sales data, not recruitment alone.
- Rank logic is versioned and deterministic.
- Correction and rebuild flows preserve history.
- Tests prove graph integrity and qualification correctness.

Output expectations:
1. Database schema or migrations
2. Domain model
3. Sponsorship graph maintenance logic
4. Qualification engine
5. Rank assignment logic
6. Admin graph correction APIs
7. Full test suite
8. Clear documentation of graph and qualification semantics
9. Notes on any assumptions or unresolved business decisions that require approval

Do not proceed to later phases in this implementation.
Do not invent future-phase systems.
Build Phase 3 as if it will be used in production immediately.